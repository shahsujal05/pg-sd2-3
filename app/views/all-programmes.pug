extends layout

block content
    h1 All Programmes
    table
        tr
            th ID
            th Programme Name
        for item in data
            tr
                td #{item.id}
                td
                    a(href='/programme-single/' + item.id) #{item.name}
